# Kubernetes deployment examples
#
# networkpolicy-task-runner.yaml — default-deny egress with allowlists for DB, OTel, DNS
# kyverno-verify-images.yaml — block unsigned GHCR images from this repo's publish workflow
#
# See docs/security/README.md for the full security program.
