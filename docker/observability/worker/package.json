{
  "name": "@devsecops/telemetry-ingest-worker",
  "version": "1.0.0",
  "private": true,
  "description": "JWT-gated Cloudflare Worker proxy for Grafana Faro ingest — closes the static public DSN attack vector",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9",
    "wrangler": "^3.114.0"
  }
}
